<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="UiButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:background">@null</item>
        <item name="android:padding">0dp</item>
        <item name="android:visibility">gone</item>
    </style>

    <style name="ProgressBar.Loading" parent="@android:style/Widget.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/animation_loading</item>
        <item name="android:minWidth">28dp</item>
        <item name="android:minHeight">28dp</item>
        <item name="android:maxWidth">28dp</item>
        <item name="android:maxHeight">28dp</item>
        <item name="android:indeterminateDuration">700</item>
    </style>

    <style name="ListphotoSelect">
        <item name="android:windowEnterAnimation">@anim/animshow</item>
        <item name="android:windowExitAnimation">@anim/animdismiss</item>
    </style>

</resources>
